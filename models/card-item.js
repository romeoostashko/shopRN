class CardItem {
  constructor(quantety, productPrice, productTitle, sum) {
    this.quantety = quantety;
    this.productPrice = productPrice;
    this.productTitle = productTitle;
    this.sum = sum;
  }
}

export default CardItem;
